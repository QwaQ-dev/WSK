<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Survey Hub</title>

    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css" />
</head>

<body>

    <div class="container-sm vh-100 d-flex justify-content-center align-items-center">
        <div class="border rounded p-4">
            <h2 class="mb-3">
                Login
            </h2>

            <form action="/admin/auth/login" method="post">
                <div class="mb-3">
                    <label for="username"
                        class="form-label">Логин</label>
                    <input type="text" class="form-control <%= violationError?.['username'] && 'is-invalid' -%>" id="username" placeholder="name@example.com"
                        name="username">

                    <div class="invalid-feedback">
                        <%= violationError?.['username'] -%>
                    </div>
                </div>

                <div class="mb-3">
                    <label for="password"
                        class="form-label">Пароль</label>
                    <input type="password" class="form-control <%= violationError?.['password'] && 'is-invalid' -%>" id="password" placeholder="*******" name="password">

                    <div class="invalid-feedback">
                        <%= violationError?.['password'] -%>
                    </div>
                </div>
            
                <% if (badRequestMessage) { %>
                    <div class="alert alert-danger" role="alert">
                       <%= badRequestMessage -%>
                    </div>
                <% } %>

                <button class="btn btn-dark w-100">
                    Войти
                </button>
            </form>
        </div>
    </div>

    <script src="/bootstrap/js/bootstrap.min.js"></script>
</body>

</html>